class Solution(object):
    def topKFrequent(self, nums, k):
        freq={}
        for num in nums:
            freq[num]=freq.get(num,0)+1
        so=sorted(freq.items(),key=lambda x:x[1],reverse=True)
        return [so[i][0] for i in range(k)]
