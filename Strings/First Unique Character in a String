class Solution(object):
    def firstUniqChar(self, s):
        d={}
        for i in s:
            d[i]=d.get(i,0)+1
        for i,char in enumerate(s):
            if d[char]==1:
                return i
        return -1
