class Solution(object):
    def vowelStrings(self, words, left, right):
        c=0
        for i in words[left:right+1]:
            if i.startswith(('a','e','i','o','u')):
                if i.endswith(('a','e','i','o','u')):
                    c+=1
        return c

# s="aeiou"
#         c=0
#         for i in range(left,right+1):
#             if words[i][0] in s and words[i][-1] in s:c+=1
#         return c
