class Solution(object):
    def finalPositionOfSnake(self, n, commands):
        row=0
        col=0
        for c in commands:
            if c=="UP":
                row-=1
            elif c=="DOWN":
                row+=1
            elif c=="RIGHT":
                col+=1
            else:
                col-=1
        return (row*n)+col
        
