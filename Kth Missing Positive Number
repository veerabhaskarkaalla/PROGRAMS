class Solution(object):
    def findKthPositive(self, arr, k):
        j = 0
        for i in range(1,arr[-1]):
            if arr[j]!=i:
                k-=1
            else:
                j+=1
            if k==0:
                return i
        return arr[-1]+k
